if (location.pathname.endsWith('/detail.html') && new URLSearchParams(location.search).get('type') === 'rapid') {
  location.replace('category.html?filter=rapid#models');
}
const menu=document.querySelector('.menu-button'),nav=document.querySelector('.mainnav');menu.onclick=()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)};document.addEventListener('keydown',e=>{if(e.key==='Escape'){nav.classList.remove('open');menu.setAttribute('aria-expanded','false');document.querySelector('.navdrop').open=false}});document.querySelectorAll('.mainnav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));const dialog=document.querySelector('dialog');document.querySelectorAll('[data-contact],[data-pending]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();if(b.hasAttribute('data-contact')){const u=new URL('contact.html',location.href);if(location.pathname.includes('category')||location.pathname.includes('detail')){u.searchParams.set('category','thermometer');const t=new URLSearchParams(location.search);const k=t.get('type')||t.get('filter')||(location.pathname.endsWith('/detail.html')?'forehead':null);if(['forehead','patch'].includes(k))u.searchParams.set('sub',k)}if(location.pathname.includes('contact')){document.querySelector('#project-form')?.scrollIntoView();return}location.href=u.href;return}const pending=b.dataset.pending;const destination={'About 公司介绍页':'about.html','About 研发内容':'about.html#research','Manufacturing & Quality':'manufacturing.html','OEM/ODM 页面':'oem.html'}[pending];if(destination){location.href=destination;return}dialog.querySelector('h2').textContent=pending?'后续页面入口':'项目询盘入口预览';dialog.querySelector('.context').textContent=pending?'目标页面：'+pending+'。该页面尚未纳入本批模板，后续继续制作。':'当前内容：'+(document.querySelector('.selected-type')?.textContent|| (location.pathname.includes('category')?'体温计':'产品项目'))+'。';dialog.querySelector('.dialog-description').textContent=pending?'该入口用于说明后续页面关系，相关模板将在下一批设计。':'正式页面将在这里进入 Contact，并带入已选品类或型号。本轮只评审三个页面模板，未连接发送服务。';dialog.showModal()}));document.querySelectorAll('.close,.close-dialog').forEach(b=>b.onclick=()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});const names={forehead:'红外额温枪',patch:'体温贴',rapid:'快速温度计'};const params=new URLSearchParams(location.search);let type=params.get('type');if(names[type]){document.querySelectorAll('.selected-type').forEach(e=>e.textContent=names[type]);document.title=names[type]+' · 型号详情模板 · YimiLife'}const filters=[...document.querySelectorAll('[data-filter]')];function filter(k){if(!names[k]&&k!=='all')k='all';filters.forEach(b=>b.setAttribute('aria-pressed',b.dataset.filter===k));document.querySelectorAll('.model-card').forEach(c=>c.hidden=k!=='all'&&c.dataset.type!==k);document.querySelector('#catalog-title').textContent=k==='all'?'Explore the range.':names[k];document.querySelector('#catalog-status').textContent=k==='all'?'2 个产品子类目 · 1 个未来产品坑位':k==='rapid'?'未来产品 · 保留展示位置':'当前子类目 · 1 个型号占位';const u=new URL(location.href);u.searchParams.set('filter',k);history.replaceState(null,'',u)}filters.forEach(b=>b.onclick=()=>filter(b.dataset.filter));if(filters.length&&params.has('filter'))filter(params.get('filter'));document.querySelectorAll('[data-image]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-image]').forEach(x=>x.setAttribute('aria-pressed',x===b));document.querySelector('#gallery-main span').textContent='['+b.dataset.image+']'});
