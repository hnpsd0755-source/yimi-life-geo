(()=>{
const workflows={oem:[['Confirm the model','提供目标品类、意向型号和市场。','[型号与配置讨论清单]'],['Define the customization','提供品牌、包装及所需配置。','[确认范围、责任和待补资料]'],['Review the sample','核对样品及与需求的对应关系。','[样品评审记录与修改项]'],['Prepare production','按项目约定确认生产与文件要求。','[生产准备及交付条件]']],odm:[['Define the requirement','提供目标使用场景、功能和市场要求。','[需求范围与约束清单]'],['Review feasibility','讨论外观、结构、功能及开发边界。','[可行性评估与阶段安排]'],['Develop & review','按约定开展原型与阶段性评审。','[原型、修改项与验证计划]'],['Confirm the next stage','评审设计输出与后续制造条件。','[后续验证及生产准备事项]']]};
const list=document.querySelector('#workflow-list');
function setPath(path){if(!workflows[path])path='oem';document.querySelectorAll('[data-path]').forEach(b=>b.setAttribute('aria-pressed',b.dataset.path===path));document.querySelector('.workflow-label').textContent=path==='oem'?'现有型号定制路径':'新产品 / 修改需求路径';list.innerHTML=workflows[path].map((s,i)=>'<article class="workflow-step"><span>0'+(i+1)+'</span><div><small>PROJECT INPUT</small><h3>'+s[0]+'</h3><p>'+s[1]+'</p></div><div class="step-output"><small>STAGE OUTPUT</small><p>'+s[2]+'</p></div></article>').join('');document.querySelector('#path-inquiry').href='contact.html?path='+path;}
if(list){setPath('oem');document.querySelectorAll('[data-path]').forEach(b=>b.onclick=()=>setPath(b.dataset.path));}
const form=document.querySelector('#project-form');if(!form)return;
const category=document.querySelector('#category'),sub=document.querySelector('#subcategory'),subField=document.querySelector('#subcategory-field');
const types={nebulizer:[['compressor','压缩雾化器'],['mesh','手持微网雾化器']],thermometer:[['forehead','红外额温枪'],['patch','体温贴'],['rapid','快速温度计']]};
function updateSub(){const data=types[category.value];subField.hidden=!data;sub.disabled=!data;sub.innerHTML='<option value="">请选择（可选）</option>'+(data||[]).map(x=>'<option value="'+x[0]+'">'+x[1]+'</option>').join('');}
category.onchange=updateSub;const query=new URLSearchParams(location.search);if(['pulse','bp','wearable','nebulizer','thermometer'].includes(query.get('category')))category.value=query.get('category');updateSub();if([...sub.options].some(o=>o.value===query.get('sub')))sub.value=query.get('sub');if(['oem','odm'].includes(query.get('path')))document.querySelector('#request').value=query.get('path')==='oem'?'希望讨论现有型号定制项目。':'希望讨论新产品或现有产品修改需求。';
const status=document.querySelector('#form-status'),submit=document.querySelector('#submit-project');const required=[...form.querySelectorAll('[required]')];
function check(el){let message='';if(!el.value.trim())message='请填写此项。';else if(el.type==='email'&&el.validity.typeMismatch)message='请输入有效的邮箱地址。';el.setAttribute('aria-invalid',String(Boolean(message)));document.getElementById(el.id+'-error').textContent=message;return !message;}
required.forEach(el=>el.addEventListener('input',()=>{if(el.getAttribute('aria-invalid')==='true')check(el)}));
form.addEventListener('submit',e=>{e.preventDefault();if(submit.disabled)return;let first=null;required.forEach(el=>{if(!check(el)&&!first)first=el});status.hidden=false;if(first){status.className='form-status error';status.textContent='请检查标记的必填项。';first.focus();return;}
status.className='form-status';status.textContent='提交中状态演示（没有发送数据）…';submit.disabled=true;submit.textContent='Previewing…';const outcome=document.querySelector('#demo-result').value;setTimeout(()=>{status.className='form-status'+(outcome==='failure'?' error':'');status.textContent=outcome==='failure'?'失败状态演示：提交未完成，请重试。已保留所有输入。没有发送数据。':'成功状态演示：真实提交后在此显示确认与下一步。本次仅本地预览，没有发送数据。';submit.disabled=false;submit.textContent='Preview submission ↗';},550);});
})();
