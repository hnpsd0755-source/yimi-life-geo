# YimiLife 跨电脑工作交接 · 2026-09-11

最新保存与续作入口：[2026-09-12 工作交接](handoff-20260912.md)。本文件保留此前过程。

## 2026-09-12 当前进度：产品总览

用户确认 Contact 并要求进入下一项。Contact 现有布局及推荐补充（品类增加“尚未确定”、可选附件沿用 ODM 格式和额度）记为已确认；这两项控件及 Contact 接口扩展尚未实施，真实收件仍待接入。

新增 products.html 及预览入口 ?page=products，八个模板按页面关系排列，当前待确认产品总览。其余四类品类页仍待制作；体温计可进入现有模板。真实图片、型号和最终文案后填。见 [产品总览记录](../design/website-templates-20260910/delivery-products-overview.md)。

下文保留各页面之前的确认过程，以本节为最新进度。

## 2026-09-12 页面确认进度

About 局部调整已实施：删除 Project coordination；页尾两个大引导区合并为制造与质量、Contact 两条紧凑内链。用户已确认 About 内容结构与 UI，资料后填。见 [调整与验证记录](../design/website-templates-20260910/delivery-about-refinement.md)。

用户已确认 OEM/ODM 基本 OK：本地页面与交互基本确认，真实询盘保存、附件存储、邮件通知及后台管理仍待实现。制造与质量已按精简版确认，当前进入 Contact 评审。页面区块与 UI 先确认，真实资料、图片及最终英文后填。

制造与质量页本轮精简已实施：保留工厂全景、两张生产现场照片、一份 ISO 13485:2016 体系证书及紧凑联系入口；删除生产流程、追溯字段和产品文件分类。原 04/05 合并为公司级体系证书展示；产品注册资料归对应型号。23 项浏览器检查通过，用户已要求进入下一项确认，制造与质量内容结构与 UI 按当前版本确认。照片与正式证书后填。见 [调整与验证记录](../design/website-templates-20260910/delivery-manufacturing-refinement.md)。

## 2026-09-12 询盘功能确认

用户已确认 OEM/ODM 六项推荐方案，见 [询盘功能与接口定义 V1.0](../plans/oem-odm-inquiry-interface-v1.md)。两区姓名、业务邮箱、公司必填；OEM Logo 可后补；ODM 允许 PDF/DOCX/图片，最多 3 个、每个 5MB；客户自动回执，内部通知共用一个邮箱；后台支持登录查看、附件下载、跟进状态和失败邮件重试。

用户随后确认继续实施。本地 OEM/ODM 预览已补齐 Logo 后补、必填联系信息、ODM 数量与附件、提交中/失败/成功及摘要；示例 Logo 不视为客户原稿。打包、类型、源码 ESLint 与 32 项主要浏览器检查通过，附件控件修正后另有 6 项补充检查通过，详见 [2026-09-12 交付记录](../design/website-templates-20260910/delivery-inquiry-preview.md)。

真实保存、邮件与后台仍未实现；服务、业务邮箱和账号在下一阶段配置。本轮变更仅在本地保存，尚未 commit/push。下文的同步与原组件一致性检查保留 2026-09-11 的历史状态。

本次保存用户已完成的官网内容规划、独立 UI 预览和建站技能，方便其他电脑继续。分支 `main`，远程 [GitHub 仓库](https://github.com/hnpsd0755-source/yimi-life-geo)。整理前代码基线：`735b4a87f12f20d9506e9e41723b4550fe0acd00`。

## 从这里继续

1. 阅读 [全站页面内容与 UI 模板定义 V1.2](../plans/yimilife-site-content-and-ui-templates-v1.2.md)。文档中的初始评审状态保留为历史；后续用户已确认按该方案推进 UI 模板。
2. 打开 [七页 UI 模板](../design/website-templates-20260910/README.md) 并启动本地预览。
3. 第一批：首页、体温计品类、型号详情的视觉方向已确认。第二批公司与研发、制造与质量、Contact 已制作，继续评审。
4. OEM/ODM 已按最新要求替换第二批简化提案：以官网原版内容、品牌配置器、流程、表单与 FAQ 为基础，仅统一 UI 风格。继续时以 `oem.html` 与 `oem-source/` 为准。
5. 后续先完成评审和剩余模板，再填核实后的资料及最终文案。正式 Next.js 页面尚未应用这些新模板；不要把预览完成理解成已上线。

## 已确定的主要方向

- B2B 海外品牌、渠道与 OEM/ODM 买家优先。先确定页面内容，再做 UI 模板，真实资料和图片后填。
- 首页主体：产品 → 公司 → 研发 → 工厂 → OEM 合作案例。客户反馈需要真实依据，不能编造客户原话。
- 五大产品类别：Pulse Oximeter、Blood Pressure Monitor、Wearable Monitoring Devices、雾化器、体温计。
- 雾化器：压缩雾化器、手持微网雾化器。体温计：红外额温枪、体温贴、快速温度计。新增类别最终英文、型号、参数和正式地址仍待确定；体温贴主分类归体温计。
- 主导航不设独立 Technology。PulseMatrix™ 只作为血氧产品的技术详情子页；low-perfusion、motion-artifact-rejection、skin-tone-fairness 是同一页中的三个方向或参数，不拆成三个专题。正式英文声明、测试条件与适用型号需核实，不能从模板推断性能结论。
- 公司与研发可以展示团队、实验室、专利；注册证书与产品/质量证据关联，Fluke 按实际测试用途介绍。
- 每次设计变化先研究需求、当前页面和相关竞品，再提出有依据的方案；按用户已有授权实施。该流程已同步到项目技能。

## 下一步与尚未完成

- 继续核对第二批模板，按内容定义补齐产品总览、其余品类、PulseMatrix™、案例/资讯等剩余模板。
- OEM/ODM 本地询盘流程已更新，ODM 已加入雾化器、体温计英文占位；最终品类英文和真实型号仍需确认。下一阶段按询盘定义配置接收服务、附件存储、业务邮箱及后台。
- 真实型号、图片、参数、证书、公司联系信息、案例正文及最终英文后填。占位数量、示例颜色和演示型号不作为产品事实。
- 询盘和上传均是本地演示，没有连接真实发送或存储服务。
- 正式路由迁移、旧 Technology 入口处理、SEO/结构化数据与正式代码集成尚未进行。

## 在其他电脑打开

首次取得项目：

```bash
git clone https://github.com/hnpsd0755-source/yimi-life-geo.git
cd yimi-life-geo
```

若已有项目，先检查 `git status --short`、`git branch --show-current` 和 `git remote -v`。只有确认当前为 `main`、远程正确且工作区干净时，执行：

```bash
git fetch origin
git merge --ff-only origin/main
```

如有未提交修改、未跟踪文件或分支异常，先停止并检查，不自动 stash、commit、rebase、reset 或普通 pull。此前办公室电脑保留的五项本地资料不应推断为其他电脑也可自动忽略；需核对具体情况。

仅查看本轮新 UI，需要 Node.js，无需安装项目依赖：

```bash
node docs/design/website-templates-20260910/serve.cjs
```

打开 http://127.0.0.1:8767/ ，OEM/ODM 为 http://127.0.0.1:8767/?page=oem 。这次办公室验证使用 8768，以保留原 8767 会话；另一台电脑默认使用 8767。

运行现有正式网站代码，首次先安装锁定依赖，再启动：

```bash
npm ci
npm run dev
```

打开 http://localhost:3000/ 。后续仅在依赖缺失或锁文件变化时重新安装。独立预览和 Next.js 网站是两个不同入口。

## Skills 与文件范围

项目 `.agents/skills` 已保留原有网站技能，并同步：

- `yimi-content-brief-to-code`、`yimi-uiux-page-review`：新增的设计前研究要求。
- `yimilife-website-growth-optimizer`：完整研究、方案、实施流程及参考文件。
- `yimilife-product-researcher`、`yimilife-project-handoff`：产品研究和跨电脑交接技能及配套资源。

在 Codex 中打开仓库后，新建任务以读取项目内技能；项目内版本随 GitHub 同步，不依赖办公室全局技能。如果其他电脑需要全局使用，可将以上新增的三个 `yimilife-*` 文件夹复制到用户的 `.codex/skills/`；已有同名版本先比较，避免覆盖本地变化，再重开任务。

[早期首页提案](../design/homepage-proposal-20260909/README.md) 仅供回顾。临时浏览器日志、旧生成脚本、技能旧备份未纳入当前成果。原先五项本地资料保持原样；`.env`、依赖目录、构建缓存未纳入本轮提交。

## 本轮实际检查

- 保存前 fetch 后，本地与远程 `main` 相同，无分叉。
- 迁移后的 OEM 预览构建成功；启动和交互脚本语法检查通过。
- 10 个 HTML 的本地链接、资源和锚点检查无缺失；29 个页面及资源请求均返回 200。
- 当前预览执行文件中未发现本机绝对路径依赖；四个原 OEM 组件/样式副本与官网源文件逐字节相同。
- 浏览器迁移验证：七页 × 1440/390px，含 OEM 内嵌页，均无横向溢出和缺图，无脚本异常；Navy 配色、示例 Logo、六项调整控件及 OEM/ODM 流程切换正常，实际改变了位置输入值（-2 → -1）。
- 仓库 TypeScript 检查通过（`tsc --noEmit --incremental false`）；新增的预览 TSX 没有引入类型错误。
- ESLint 对独立预览的首次检查发现其 CommonJS 适配代码及已编译第三方代码与应用规则不兼容，因此仅在 `eslint.config.mjs` 中排除 `docs/design/website-templates-20260910/**`；该目录通过自身构建、语法、类型与浏览器检查。
- 未修改 `app/`、`components/`、生产图片或项目依赖/部署配置。此次未运行全站 Next.js build，验证范围是迁移后的独立预览、类型兼容性与文档。

## 给下一台电脑 Codex 的提示

> 继续 YimiLife 官网项目。先读 AGENTS.md、docs/workflows/handoff-20260911.md、内容定义 V1.2 和项目内相关 skills。先检查 Git 状态和分支；工作区干净且分支正常才 fetch 并 fast-forward 更新。启动 docs/design/website-templates-20260910/serve.cjs，打开当前七页模板，OEM/ODM 以原版复用的最新预览为准。先汇报当前状态与待办，再按我指示继续。设计变更前研究需求和竞品、提出方案；不要编造资料。未经后续明确要求，不 commit、push 或部署。
